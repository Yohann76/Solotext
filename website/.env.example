# URL de l'application (pour les liens de navigation)
VITE_APP_URL=http://localhost:8090

# URL de l'API backend (optionnel, utilise /api par défaut si non défini)
# VITE_API_URL=http://localhost:3001/api
